# edit
A simple python script that opens a text document in the default text editor.

simply invoke the command `edit` and provide a filename in the current directory, or a path to a filename.

Depends on xdg.
